version: "3.8"

services:
  chat-server-blue:
    image: ${ECR_HOST}/${ECR_APP_NAME}:${IMAGE_TAG}
    container_name: napzak-chat-blue-prod

    ports:
      - "8080:8080"

    environment:
      FIREBASE_CONFIG_PATH: file:/app/firebase/napzak-firebase-adminsdk.json

    volumes:
      - /home/ec2-user/Napzak-BE/secrets/prod/chat/application-prod.yml:/app/application-prod.yml:ro
      - /home/ec2-user/Napzak-BE/secrets/prod/chat/napzak-firebase-adminsdk.json:/app/firebase/napzak-firebase-adminsdk.json:ro

    networks:
      - napzak-prod-net

    restart: always

networks:
  napzak-prod-net:
    external: true